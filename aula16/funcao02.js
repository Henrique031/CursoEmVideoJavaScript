// function soma (n1, n2) { // Meu parametro é n1, n2, e se chama soma.
function soma (n1=0, n2=0) { // Parametros já pré-definidos // Se o parametro 1 ou 2 não for informado(chamdo) ele vai ser 0
    return n1 + n2  // 5 + 5 = 10 (vai me retonar 10 como resposta)
}
 console.log(soma(5, )) // O segundo valor não é informado, vai aparecer NaN(não é um núemro)
//  console.log(soma(5, 5)) // O primeiro 5 vai para (n1) e o segundo 5 vai para (n3)

/*let res = soma(5, 5) // O primeiro 5 vai para (n1) e o segundo 5 vai para (n3)
console.log(res)
*/